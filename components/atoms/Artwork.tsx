import React from "react";

const Artwork: React.FC = () => {
  return (
    <></>
    // <button
    //   className="card"
    //   style={{ backgroundImage: `url(art/${art}.webp` }}
    //   aria-label={art}
    //   onClick={() => (clicked = true)}
    //   onKeyDown={() => (clicked = true)}
    // ></button>
  );
};

export default Artwork;
